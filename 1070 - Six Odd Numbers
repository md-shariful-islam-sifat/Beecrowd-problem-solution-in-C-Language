#include<stdio.h>
int main()
{
    int i,x,rngoddsix=6;

    scanf("%d",&x);

    for(i=x; i<(x+(rngoddsix*2)); i=i+2)
    {

        if(i%2==0)
        {
            i=i+1;
            printf("%d\n",i);
        }
        else
        {
            printf("%d\n",i);
        }
    }
    return 0;
}
