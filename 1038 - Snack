#include<stdio.h>
int main()
{
    int a,b;
    float s;

    scanf("%d %d",&a,&b);

    if(a==1){
        s=b*4.00;
    }
    if(a==2){
        s=b*4.50;
    }
    if(a==3){
        s=b*5.00;
    }
    if(a==4){
        s=b*2.00;
    }
    if(a==5){
        s=b*1.50;
    }

    printf("Total: R$ %.2f\n",s);
    return 0;
}
