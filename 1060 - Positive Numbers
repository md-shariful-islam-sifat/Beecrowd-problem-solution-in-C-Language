#include<stdio.h>
int main()
{
    float n,count=0,i;

    for(i=1;i<=6;i++)
    {
        scanf("%f",&n);

        if(n>0)
        {
            count++;
        }
    }
    printf("%.f valores positivos\n",count);

    return 0;
}
